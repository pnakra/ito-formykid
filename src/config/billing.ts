// Single place to restore payment gating later.
// While true, every feature is free and unlimited for all users.
export const is_free_tier_unlimited = true;

// Kept for when gating is restored.
export const FREE_LOOKUP_LIMIT = 3;
